# Deployment Guide

## Heroku

Use `heroku.yml` for Docker-based deployment.

## Docker

```bash
docker build -t charlton-xmd .
docker run charlton-xmd
```

## Environment Setup

Configure `.env` with your WhatsApp credentials.
